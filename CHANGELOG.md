# Changelog

## Unreleased

- Store all Quality Studio runtime state in a configurable external project data
  root, migrate legacy in-tree `.quality` data once, warn about checkout
  contamination at startup, and reserve source control for explicit user exports.
