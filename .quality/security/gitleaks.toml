title = "Quality Studio Gitleaks configuration"

[extend]
useDefault = true

[allowlist]
description = "Generated build output and scanner-owned security metadata are not scanned for secret findings."
paths = [
  '''(^|/)bin(/|$)''',
  '''(^|/)obj(/|$)''',
  '''(^|/)dist(/|$)''',
  '''(^|/)coverage(/|$)''',
  '''(^|/)node_modules(/|$)''',
  '''(^|/)\.angular(/|$)''',
  '''(^|/)\.quality/security(/|$)''',
]
