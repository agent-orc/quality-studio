bin/
obj/
TestResults/
src/QualityStudio.Api/.quality-studio/
*.user
*.suo
.vs/

# Orchestrator jobs live in the task-server workspace, never in the product repo (operator decision 2026-07-11)
.orchestrator/
bin/
obj/
.vs/
TestResults/

# orchestrator task-pipeline artifacts stay out of the repo
/.orchestrator/

# Quality Studio runtime state belongs in its configured application data root.
# Ignore legacy root and per-folder trees that have not yet been migrated.
**/.quality/
/.dev-stack.log
