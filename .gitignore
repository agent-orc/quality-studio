bin/
obj/
TestResults/
*.user
*.suo
.vs/

# Orchestrator jobs live in the task-server workspace, never in the product repo (operator decision 2026-07-11)
.orchestrator/
.quality/runs/
/.quality/preflight/
bin/
obj/
.vs/
TestResults/

# orchestrator task-pipeline artifacts stay out of the repo
/.orchestrator/

# Quality Studio runtime state is stored outside analysed checkouts. This pattern
# also protects repositories that previously received per-folder review sidecars.
.quality/
/.dev-stack.log
