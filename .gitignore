bin/
obj/
TestResults/
src/QualityStudio.Api/.quality-studio/
*.user
*.suo
.vs/

# Orchestrator jobs live in the task-server workspace, never in the product repo (operator decision 2026-07-11)
.orchestrator/

# Quality Studio runtime state belongs in its external project data root. Ignore
# legacy or accidentally recreated in-tree data at the root and in subfolders.
.quality/
**/.quality/

/.dev-stack.log
