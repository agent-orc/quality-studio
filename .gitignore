bin/
obj/
TestResults/
src/QualityStudio.Api/.quality-studio/
*.user
*.suo
.vs/

# Orchestrator jobs live in the task-server workspace, never in the product repo (operator decision 2026-07-11)
.orchestrator/
.quality/runs/
bin/
obj/
.vs/
TestResults/

# orchestrator task-pipeline artifacts stay out of the repo
/.orchestrator/

# Review usage, finding policy, terminal evidence, and canonical run reports are
# durable repository history. Only resumable orchestration under .quality/runs is ignored.
!/.quality/
!/.quality/usage/
!/.quality/usage/*.jsonl
!/.quality/findings/
!/.quality/findings/**
!/.quality/review-history/
!/.quality/review-history/**
