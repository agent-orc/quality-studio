// Fixed literal sizes only. Relative CSS sizes require rendered UI review.
// Exceptions must name an exact absolute CSS file, selector, property and a reason.
export const typographyContract = {
  minimumPx: 11,
  tokenPrefixes: ['--studio-font-size-'],
  exceptions: [],
};
