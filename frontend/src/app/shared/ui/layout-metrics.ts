/** Shared by virtualized layout calculations and their component host CSS properties. */
export const WORKSPACE_METRICS = {
  codeLine: 24,
  explorerRow: 32,
  tableRow: 44,
  threadCollapsed: 40,
  threadExpandedMin: 220,
  threadHeader: 88,
  threadEntry: 64,
  composer: 128,
} as const;
