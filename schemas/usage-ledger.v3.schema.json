{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-orchestrator.dev/quality/schemas/usage-ledger.v3.schema.json",
  "title": "Quality Studio usage ledger entry v3",
  "description": "One agent-backed review operation. Version 3 attributes every operation to a model source so cost and quality evidence always name a real model; the sweep id is optional because standalone CLI reviews have none.",
  "type": "object",
  "additionalProperties": false,
  "required": ["runId", "timestamp", "model", "cliType", "tokens", "kind", "level", "path", "schemaVersion", "modelSource"],
  "properties": {
    "runId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200,
      "description": "Provider or CLI run identifier for this individual review operation."
    },
    "timestamp": { "type": "string", "format": "date-time" },
    "model": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200,
      "description": "The model id the CLI was asked to run. The literal \"runner-default\" appears only together with modelSource \"runner-default\"."
    },
    "cliType": { "type": "string", "minLength": 1, "maxLength": 100 },
    "tokens": { "$ref": "#/$defs/tokens" },
    "kind": { "type": "string", "minLength": 1, "maxLength": 100 },
    "level": { "type": "string", "minLength": 1, "maxLength": 100 },
    "path": { "type": "string", "minLength": 1 },
    "reviewRunId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200,
      "description": "Durable sweep/job identifier shared by every operation in one review run. Absent for standalone CLI reviews."
    },
    "schemaVersion": { "const": 3 },
    "modelSource": {
      "enum": ["explicit", "policy-default", "runner-default"],
      "description": "explicit: the caller named the model. policy-default: no model was named and Quality Studio resolved the synchronized routing policy's route for the CLI and passed it explicitly. runner-default: no model was named and the policy routes nothing for the CLI, so the CLI's own default served the run and is unknown."
    },
    "cost": { "$ref": "#/$defs/cost" }
  },
  "$defs": {
    "cost": {
      "type": "object",
      "additionalProperties": false,
      "required": ["total", "currency", "status"],
      "description": "Estimated provider cost at the catalog price valid at the operation timestamp. total is null when no price resolved; status names the reason (resolved, unknownModel, noPriceForDate).",
      "properties": {
        "total": { "type": ["number", "null"], "minimum": 0 },
        "currency": { "type": ["string", "null"], "minLength": 1, "maxLength": 10 },
        "status": { "type": "string", "minLength": 1, "maxLength": 50 }
      }
    },
    "tokens": {
      "type": "object",
      "additionalProperties": false,
      "required": ["inputTokens", "outputTokens", "cachedInputTokens", "reasoningOutputTokens", "durationMs"],
      "properties": {
        "inputTokens": { "type": ["integer", "null"], "minimum": 0 },
        "outputTokens": { "type": ["integer", "null"], "minimum": 0 },
        "cachedInputTokens": { "type": ["integer", "null"], "minimum": 0 },
        "reasoningOutputTokens": { "type": ["integer", "null"], "minimum": 0 },
        "durationMs": { "type": "integer", "minimum": 0 }
      }
    }
  }
}
